=等号
#变量赋值
a=28
#字符串比较
if[ "str1" = "str2" ]
#算术操作符
gcd()
{
    str1=$1
    str2=$2
}
gcd $1 $2
 
#逻辑操作符
#！非
#&&与
#||或

#操作符优先级
先看轮廓，再看细节
#最佳策略：增加一些方括号，方便阅读
if[ "$v1" -gt "$v2" -o "$v1"  -lt "$v2" -a -e "$filename" ] 

if[[ "$v1" -gt "$v2"]] || [["$v1"  -lt "$v2" ]] &&  [[ -e "$filename" ]]

数字常量
8进制：以0开头
let "oct = 032"

16进制：以0x开头
let "hex=0x32"

